import ExploreMenu from "./ExploreMenu"

export default{
    title:"organisms/ExploreMenu"
}

const Template = (args) => <ExploreMenu {...args}/>

export const Menu = Template.bind({})

Menu.args={
    
}